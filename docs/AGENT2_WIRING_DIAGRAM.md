# Agent 2.0 Complete Wiring Diagram

## 📊 Visual Architecture Map

This diagram shows the complete wiring of Agent 2.0 from UI to Runtime to Database.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          AGENT 2.0 CONTROL PLANE UI                         │
│                     (public/js/ai-agent-settings/Agent2Manager.js)          │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       │ User configures via 3 tabs:
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
                    ▼                  ▼                  ▼
        ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
        │ Configuration   │  │   Greetings     │  │  Call Review    │
        │      Tab        │  │      Tab        │  │      Tab        │
        ├─────────────────┤  ├─────────────────┤  ├─────────────────┤
        │ • Discovery     │  │ • Call Start    │  │ • Call List     │
        │ • Style         │  │ • Interceptor   │  │ • Call Details  │
        │ • Vocabulary    │  │ • Intent Words  │  │ • Transcripts   │
        │ • Clarifiers    │  │ • Rules         │  │ • Events        │
        │ • Playbook      │  │                 │  │                 │
        │ • Trigger Cards │  │                 │  │                 │
        │ • Fallbacks     │  │                 │  │                 │
        └─────────────────┘  └─────────────────┘  └─────────────────┘
                    │                  │                  │
                    └──────────────────┼──────────────────┘
                                       │
                                       │ Save (PATCH)
                                       ▼
        ┌──────────────────────────────────────────────────────────┐
        │              ADMIN API LAYER                             │
        │         (routes/admin/agent2.js)                         │
        ├──────────────────────────────────────────────────────────┤
        │ GET    /api/admin/agent2/:companyId                      │
        │ PATCH  /api/admin/agent2/:companyId                      │
        │ POST   /api/admin/agent2/:companyId/gpt-prefill          │
        │ POST   /api/admin/agent2/:companyId/greetings/seed       │
        │ GET    /api/admin/agent2/calls/:companyId                │
        │ GET    /api/admin/agent2/calls/:companyId/:callSid       │
        └──────────────────────────────────────────────────────────┘
                                       │
                                       │ Write to DB + Invalidate cache
                                       ▼
        ┌──────────────────────────────────────────────────────────┐
        │                  DATABASE LAYER                          │
        │              (models/v2Company.js)                       │
        ├──────────────────────────────────────────────────────────┤
        │ MongoDB Collection: companies                            │
        │ Document Path: aiAgentSettings.agent2                    │
        │                                                          │
        │ Schema:                                                  │
        │ {                                                        │
        │   enabled: Boolean,                                      │
        │   discovery: { ... },  ← All Discovery tab config        │
        │   greetings: { ... },  ← All Greetings tab config        │
        │   meta: { uiBuild: ... }                                 │
        │ }                                                        │
        └──────────────────────────────────────────────────────────┘
                                       │
                                       │ Cache invalidation
                                       ▼
        ┌──────────────────────────────────────────────────────────┐
        │                  REDIS CACHE LAYER                       │
        │                  (clients/redis.js)                      │
        ├──────────────────────────────────────────────────────────┤
        │ Key: company-phone:{phoneNumber}                         │
        │ Action: DEL on config save → Force fresh DB read         │
        │ Purpose: Twilio runtime picks up new config immediately  │
        └──────────────────────────────────────────────────────────┘
                                       │
                                       │ Twilio incoming call
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            RUNTIME LAYER                                    │
│                     (Twilio Call Processing Flow)                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       │ POST /api/twilio/v2-agent-respond/:companyID
                                       ▼
        ┌──────────────────────────────────────────────────────────┐
        │           TWILIO ROUTE HANDLER                           │
        │            (routes/v2twilio.js)                          │
        ├──────────────────────────────────────────────────────────┤
        │ 1. Fetch company config (from Redis or DB)               │
        │ 2. Initialize call (CallSummaryService)                  │
        │ 3. Route to appropriate handler:                         │
        │    • After-hours → AfterHoursCallTurnHandler             │
        │    • Vendor-first → VendorCallTurnHandler                │
        │    • Normal → ConversationEngine.processTurn()           │
        └──────────────────────────────────────────────────────────┘
                                       │
                                       │ Normal call flow
                                       ▼
        ┌──────────────────────────────────────────────────────────┐
        │          FRONT DESK CORE RUNTIME                         │
        │    (services/engine/FrontDeskCoreRuntime.js)             │
        ├──────────────────────────────────────────────────────────┤
        │ S1    Runtime Ownership (set lane)                       │
        │ S1.5  Connection Quality Gate (hello? detection)         │
        │ S2    Input Text Truth (log what we got)                 │
        │ S2.5  Escalation Detection (manager requests)            │
        │ ┌──────────────────────────────────────────────────────┐ │
        │ │ GREET  Greeting Intercept                           │ │
        │ │        ↓                                            │ │
        │ │   Agent 2.0 Enabled?                               │ │
        │ │   YES → Agent2GreetingInterceptor ✅                │ │
        │ │   NO  → GreetingInterceptor (legacy)               │ │
        │ └──────────────────────────────────────────────────────┘ │
        │ S3    Slot Extraction (name/phone/address)               │
        │ ┌──────────────────────────────────────────────────────┐ │
        │ │ S4    Discovery Flow                                │ │
        │ │       ↓                                             │ │
        │ │   Agent 2.0 Discovery Enabled?                      │ │
        │ │   YES → Agent2DiscoveryRunner ✅ (ONE MIC)          │ │
        │ │   NO  → DiscoveryFlowRunner (legacy)               │ │
        │ └──────────────────────────────────────────────────────┘ │
        │ S5    Consent Gate (intent → booking consent)            │
        │ S6    Booking Flow (collect remaining info)              │
        │ S7    Voice Provider (TTS output)                        │
        │ OPEN  Opener Engine (prepend micro-ack)                  │
        └──────────────────────────────────────────────────────────┘
                                       │
                                       │ If Agent 2.0 Discovery enabled
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    AGENT 2.0 SERVICE LAYER (S4 - Discovery)                 │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│               Agent2DiscoveryRunner.js                                      │
│     (services/engine/agent2/Agent2DiscoveryRunner.js)                       │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1. Fetch Agent 2.0 config from company.aiAgentSettings.agent2              │
│ 2. If vocabulary enabled → Run Agent2VocabularyEngine                       │
│ 3. Match against trigger cards via TriggerCardMatcher                       │
│ 4. If no match + clarifier hint → Ask clarifying question                   │
│ 5. If no match + scenario fallback enabled → Run ScenarioEngine             │
│ 6. If no match → Use fallback response                                      │
│ 7. Return response text + next action                                       │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ├─────────────────────────────────────────────────┐
        │                                                 │
        ▼                                                 ▼
┌──────────────────────┐                    ┌──────────────────────┐
│ Agent2VocabularyEngine│                    │ TriggerCardMatcher   │
│ .js                  │                    │ .js                  │
├──────────────────────┤                    ├──────────────────────┤
│ • HARD_NORMALIZE     │                    │ • Match keywords     │
│   (acee → ac)        │                    │ • Match phrases      │
│ • SOFT_HINT          │                    │ • Check negatives    │
│   (thingy → maybe_   │                    │ • Priority sorting   │
│    thermostat)       │                    │ • Return best match  │
└──────────────────────┘                    └──────────────────────┘

                                       │ If greeting detected
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    AGENT 2.0 SERVICE LAYER (GREET)                          │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│            Agent2GreetingInterceptor.js                                     │
│     (services/engine/agent2/Agent2GreetingInterceptor.js)                   │
├─────────────────────────────────────────────────────────────────────────────┤
│ 1. Check input length ≤ maxWordsToQualify (default 2 words)                │
│ 2. Check for intent words (blocks greeting if found)                        │
│ 3. Match input against greeting rules (hi/hello/good morning)               │
│ 4. Return instant response OR null (pass through to discovery)              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                       EXPORT SYSTEM (NEW!)                                  │
└─────────────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│     Agent2Manager._generateComprehensiveWiringReport()                      │
│     (public/js/ai-agent-settings/Agent2Manager.js)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ INPUT:                                                                      │
│ • this.config (current UI state including unsaved changes)                  │
│ • this.companyId                                                            │
│ • this.isDirty (has unsaved changes?)                                       │
│                                                                             │
│ PROCESS:                                                                    │
│ 1. Build Configuration Snapshot (all tabs)                                  │
│ 2. Map Runtime Integration (services, flow, APIs)                           │
│ 3. Run Validation (critical, warnings, recommendations)                     │
│ 4. Calculate Statistics (counts, uniques, breakdowns)                       │
│ 5. Add Export Metadata (timestamp, version, flags)                          │
│                                                                             │
│ OUTPUT:                                                                     │
│ {                                                                           │
│   _README: { ... },                    ← Human-readable guide               │
│   metadata: { ... },                   ← Export info                        │
│   configuration: {                     ← ALL CONFIG (all tabs)              │
│     discovery: { ... },                                                     │
│     greetings: { ... }                                                      │
│   },                                                                        │
│   runtimeIntegration: {                ← HOW IT'S WIRED                     │
│     speakerOwnership: { ... },                                              │
│     services: { ... },                                                      │
│     coreRuntimeIntegration: { ... },                                        │
│     apiEndpoints: { ... }                                                   │
│   },                                                                        │
│   validation: {                        ← HEALTH CHECKS                      │
│     criticalIssues: [ ... ],                                                │
│     warnings: [ ... ],                                                      │
│     recommendations: [ ... ]                                                │
│   },                                                                        │
│   statistics: { ... }                  ← STATS & INSIGHTS                   │
│ }                                                                           │
│                                                                             │
│ DELIVERY:                                                                   │
│ • File download: agent2-wiring-complete-{companyId}-{date}.json            │
│ • Clipboard copy (for quick paste)                                          │
│ • Success alert with summary                                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 🎯 Key Integration Points

### 1. UI → API → Database
```
User edits config in UI
  → Click Save
  → PATCH /api/admin/agent2/:companyId
  → Write to MongoDB companies.aiAgentSettings.agent2
  → Delete Redis cache company-phone:{phoneNumber}
  → Success
```

### 2. Twilio Call → Runtime → Agent 2.0
```
Incoming call
  → POST /api/twilio/v2-agent-respond/:companyID
  → Fetch company config (Redis or DB)
  → FrontDeskCoreRuntime.processTurn()
  → GREET: Agent2GreetingInterceptor (if enabled)
  → S4: Agent2DiscoveryRunner (if enabled)
      → VocabularyEngine (if enabled)
      → TriggerCardMatcher
      → Clarifiers (if hint found)
      → ScenarioEngine (if fallback enabled)
      → Fallback response
  → Return TwiML
```

### 3. Export Flow
```
User clicks "Download Complete Wiring Report"
  → _readFormIntoConfig() (capture unsaved changes)
  → _generateComprehensiveWiringReport()
      → Build configuration snapshot
      → Map runtime integration
      → Run validation checks
      → Calculate statistics
      → Add metadata
  → JSON.stringify()
  → Download file + Copy to clipboard
  → Success alert
```

## 📊 Data Flow Diagram

```
┌─────────────┐
│   UI Tab    │ User edits
│ (Discovery) │────────┐
└─────────────┘        │
                       │
┌─────────────┐        │
│   UI Tab    │ User edits
│ (Greetings) │────────┼──→ this.config (in-memory)
└─────────────┘        │
                       │
┌─────────────┐        │
│   UI Tab    │ User views
│(Call Review)│────────┘
└─────────────┘
        │
        │ Click Save
        ▼
┌─────────────────────────────────────┐
│ API: PATCH /api/admin/agent2/       │
│                                     │
│ 1. Merge with defaults              │
│ 2. Validate structure                │
│ 3. Write to MongoDB                  │
│ 4. Invalidate Redis cache            │
│ 5. Log to ConfigAuditService         │
│ 6. Log to BlackBoxLogger             │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│ MongoDB: companies collection       │
│ Path: aiAgentSettings.agent2        │
│                                     │
│ Document persisted ✅               │
└─────────────────────────────────────┘
        │
        │ Next incoming call
        ▼
┌─────────────────────────────────────┐
│ Twilio: POST /v2-agent-respond      │
│                                     │
│ Fetch company (Redis or MongoDB)    │
└─────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────┐
│ FrontDeskCoreRuntime                │
│                                     │
│ IF agent2.enabled && discovery.enabled:
│   → Agent2DiscoveryRunner ✅        │
│ ELSE:                               │
│   → DiscoveryFlowRunner (legacy)    │
└─────────────────────────────────────┘
```

## 🔄 One Mic Architecture (Agent 2.0 Takeover)

```
                    ┌─────────────────────┐
                    │ Incoming caller     │
                    │ input text          │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────┐
                    │ Is Agent 2.0        │
                    │ enabled?            │
                    └──────────┬──────────┘
                               │
                    ┌──────────▼──────────────────────┐
                    │          YES                    │
                    │                                 │
            ┌───────▼───────┐           ┌────────────▼─────────┐
            │ Greeting?     │           │ Discovery input      │
            │ (hi/hello)    │           │                      │
            └───────┬───────┘           └────────────┬─────────┘
                    │                                │
        ┌───────────▼───────────┐       ┌────────────▼──────────┐
        │ Agent2Greeting        │       │ Agent2Discovery       │
        │ Interceptor           │       │ Runner                │
        │                       │       │                       │
        │ ONE MIC ✅            │       │ ONE MIC ✅            │
        │ No fallback to legacy │       │ No fallback to legacy │
        └───────────┬───────────┘       └────────────┬──────────┘
                    │                                │
                    └────────────┬───────────────────┘
                                 │
                    ┌────────────▼──────────┐
                    │ Return response       │
                    │ to FrontDeskCore      │
                    └───────────────────────┘

                              NO
                    ┌──────────▼──────────┐
                    │ Legacy System       │
                    │ (DiscoveryFlowRunner│
                    │ + GreetingInterceptor)│
                    └─────────────────────┘
```

**KEY INSIGHT**: When Agent 2.0 is enabled, it owns the mic. No competition, no fallback, no confusion.

## 🎯 Export System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                    EXPORT BUTTON CLICK                          │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│  1. _readFormIntoConfig(container)                              │
│     ↓                                                           │
│  Reads ALL form fields from UI (including unsaved changes):     │
│  • Discovery: ackWord, forbid phrases, trigger cards, etc.      │
│  • Greetings: call start, interceptor rules, etc.               │
│  • Updates this.config with current state                       │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│  2. _generateComprehensiveWiringReport()                        │
│     ↓                                                           │
│  SECTION 1: Configuration Snapshot                             │
│  • Walk this.config recursively                                 │
│  • Add _stats fields (counts, flags, status)                    │
│  • Organize by tab (discovery, greetings)                       │
│     ↓                                                           │
│  SECTION 2: Runtime Integration                                │
│  • Check config.enabled flags                                   │
│  • Map to service files                                         │
│  • Show execution flow position                                 │
│  • List API endpoints                                           │
│     ↓                                                           │
│  SECTION 3: Validation                                          │
│  • Run 12+ health checks                                        │
│  • Detect critical issues                                       │
│  • Generate warnings                                            │
│  • Add recommendations                                          │
│     ↓                                                           │
│  SECTION 4: Statistics                                          │
│  • Count trigger cards, keywords, phrases                       │
│  • Calculate unique values                                      │
│  • Group by priority                                            │
│  • Count vocabulary/clarifier entries                           │
│     ↓                                                           │
│  SECTION 5: Metadata                                            │
│  • Add timestamp                                                │
│  • Add export version                                           │
│  • Add company ID                                               │
│  • Add dirty state flag                                         │
│     ↓                                                           │
│  Return complete JSON object                                    │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│  3. JSON.stringify(comprehensive, null, 2)                      │
│     ↓                                                           │
│  Pretty-print with 2-space indent                               │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│  4. File Download                                               │
│     ↓                                                           │
│  • Create Blob from JSON string                                 │
│  • Generate filename: agent2-wiring-complete-{id}-{date}.json   │
│  • Create download link                                         │
│  • Trigger click                                                │
│  • Cleanup (revoke URL)                                         │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│  5. Clipboard Copy                                              │
│     ↓                                                           │
│  navigator.clipboard.writeText(payload)                         │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│  6. Success Alert                                               │
│     ↓                                                           │
│  Show summary: Downloaded + Copied + What's included            │
└─────────────────────────────────────────────────────────────────┘
```

## 🎓 How to Read This Diagram

1. **Start at the top**: User in Control Plane UI
2. **Follow the arrows**: Data flows downward
3. **Blue boxes**: UI components
4. **Green boxes**: Backend API/services
5. **Purple boxes**: Database/cache
6. **Orange boxes**: Runtime processing
7. **Yellow boxes**: Export system (NEW!)

---

**Legend**:
- ✅ = Agent 2.0 active path
- ❌ = Legacy path (when Agent 2.0 disabled)
- → = Data flow direction
- ▼ = Sequential step
- ┌─┐ = System boundary

---

**Related Documentation**:
- `docs/AGENT2_COMPLETE_WIRING_EXPORT.md` - Full export documentation
- `docs/AGENT2_EXPORT_QUICK_START.md` - Quick start guide
- `docs/RUNTIME_ARCHITECTURE.md` - Overall runtime architecture
