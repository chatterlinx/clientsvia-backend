/**
 * ============================================================================
 * CONTROL PLANE ENFORCER - DEPRECATED STUB
 * ============================================================================
 * This module has been deprecated and removed. This stub exists only to
 * prevent runtime crashes from legacy imports.
 * 
 * DO NOT ADD NEW CODE THAT USES THIS MODULE.
 * ============================================================================
 */

module.exports = {
  CONTRACT: { version: 'DEPRECATED' },
  CONTRACT_KEYS: new Set(),
  enforce: () => ({ valid: true }),
  validate: () => ({ valid: true }),
};
